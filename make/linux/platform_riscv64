os_family = linux

arch = riscv

arch_model = riscv_64

os_arch = linux_riscv

os_arch_model = linux_riscv_64

lib_arch = riscv64

compiler = gcc

gnu_dis_arch = riscv64

sysdefs = -DLINUX -D_GNU_SOURCE -DRISCV64
